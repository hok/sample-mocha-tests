import chai from 'chai';
const { expect } = chai;

describe('Passing sample tests', function() {
  it('one should be equal to one', function() {
    expect(1).to.equal(1);
  });

  it('two should be equal to two', function() {
    expect(2).to.equal(2);
  });
});